const demos=[
 {slug:'pilote-identite-numerique',title:'Identité numérique : les questions à poser avant de parler d’impact',country:'Côte d’Ivoire',sector:'Services publics',format:'Analyse',date:'20.07.26',summary:'Comment distinguer une annonce, un déploiement et un résultat réellement mesuré.',body:'Cette démonstration illustre la structure éditoriale attendue sans présenter une actualité comme établie.',confirmed:'Le protocole exige une source primaire, une date, un périmètre et un responsable identifiés.',unknown:'Les chiffres d’adoption, le calendrier et les garanties restent à documenter.',analysis:'La qualité d’un service numérique public se mesure à son accessibilité, sa sécurité et son usage réel.',demo:true,sources:[{level:'A',name:'Source primaire à documenter'}]},
 {slug:'pilote-agritech-preuve',title:'Agritech : passer du prototype à la preuve d’usage',country:'Bénin',sector:'Agriculture',format:'Étude de cas',date:'18.07.26',summary:'Documenter le problème, la solution, le déploiement et les limites sur le terrain.',body:'Un exemple de la méthode appliquée à une étude de cas.',confirmed:'Chaque résultat doit être relié à une période, un échantillon et une source.',unknown:'Les données terrain et les témoignages doivent être corroborés.',analysis:'Le test décisif est la continuité d’usage sur une saison complète.',demo:true,sources:[{level:'A',name:'Jeu de données original à documenter'}]},
 {slug:'pilote-fintech-interoperabilite',title:'Fintech : ce que change vraiment l’interopérabilité',country:'Sénégal',sector:'Fintech',format:'Décryptage',date:'16.07.26',summary:'Les critères à vérifier derrière un mot souvent utilisé trop rapidement.',body:'Un décryptage pilote qui sépare la connexion technique du bénéfice utilisateur.',confirmed:'L’interopérabilité suppose des règles techniques, opérationnelles et économiques.',unknown:'Acteurs, frais, délais et recours doivent être confirmés pour chaque système.',analysis:'Une connexion n’est utile que si elle réduit réellement la friction.',demo:true,sources:[{level:'A',name:'Documentation réglementaire à documenter'}]}
];
const main=document.querySelector('#main'),state={filter:'Tous',tab:'overview',signals:[],sources:[],articles:[],publicArticles:[],publicLoaded:false,loading:false,refreshing:false,editorSignal:null,editorArticle:null,filters:{country:'',source:'',sector:'',status:'',period:'30',sort:'newest'}};
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const toast=s=>{const e=document.querySelector('.toast');e.textContent=s;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2400)};
function schema(article){document.querySelector('#structured-data').textContent=JSON.stringify({'@context':'https://schema.org','@type':article?'Article':'WebSite',name:article?.title||'Africa Digital Pulse',headline:article?.title,url:'https://pulse.bookemrick.online/',author:{'@type':'Person',name:'Gabriel Emrick DAHISSIHO',url:'https://pulse.bookemrick.online/#/auteur'},inLanguage:'fr'})}
function story(a,i){return `<a class="story" href="/#/article/${a.slug}"><span class="story-index">SIGNAL / 0${i+1}</span><h3>${esc(a.title)}</h3><p>${esc(a.summary)}</p><div class="story-foot"><span class="story-meta">${esc(a.country)}<br>${esc(a.sector)} · ${esc(a.format)}</span><span class="story-arrow">↗</span></div></a>`}
function publicCorpus(){return state.publicArticles.length?state.publicArticles.map(a=>({...a,format:'Analyse',date:fmtDate(a.published_at),unknown:a.unknown_facts,sources:a.source_name?[{level:'A',name:a.source_name,url:a.source_url}]:[]})):demos}
async function loadPublicArticles(){if(state.publicLoaded)return;try{const r=await fetch('/api/public/articles');if(!r.ok)throw new Error(`API ${r.status}`);const data=await r.json();state.publicArticles=data.items||[]}catch(e){console.error('Articles publics indisponibles',e)}finally{state.publicLoaded=true}}
function home(){schema();const corpus=publicCorpus(),fs=['Tous',...new Set(corpus.flatMap(a=>[a.country,a.sector]).filter(Boolean))];const list=state.filter==='Tous'?corpus:corpus.filter(a=>a.country===state.filter||a.sector===state.filter);main.innerHTML=`<section class="hero"><div class="hero-grid"></div><p class="kicker"><span>●</span> TECHNOLOGIE · AFRIQUE FRANCOPHONE · PREUVES</p><h1>Le signal<br>derrière le <em>bruit.</em></h1><div class="hero-bottom"><p class="hero-intro">L’innovation africaine mérite mieux qu’un communiqué recopié. Nous <strong>vérifions les faits</strong>, exposons les limites et expliquons ce qui change vraiment.</p><div class="hero-stat"><div><b>${String(corpus.length).padStart(2,'0')}</b>articles publics</div><div><b>100%</b>validation humaine</div></div></div></section><section class="section"><div class="section-head"><div><p class="kicker">01 — DERNIERS DÉCRYPTAGES</p><h2>À lire<br>maintenant.</h2></div><p>Les articles approuvés dans le Studio apparaissent ici automatiquement.</p></div><div class="filter-row">${fs.map(f=>`<button class="chip ${f===state.filter?'active':''}" data-filter="${esc(f)}">${esc(f)}</button>`).join('')}</div><div class="story-grid">${list.map(story).join('')}</div></section><section class="proof section"><p class="kicker">02 — NOTRE DIFFÉRENCE</p><h2 class="proof-title">Pas de vitesse<br>sans <em>preuve.</em></h2><div class="proof-steps"><div class="proof-step"><b>01 / COLLECTER</b><h3>Sources identifiées</h3><p>Flux publics, institutions et médias spécialisés, jamais de contournement.</p></div><div class="proof-step"><b>02 / RECOUPER</b><h3>Faits atomiques</h3><p>Chaque affirmation importante est reliée à une preuve datée.</p></div><div class="proof-step"><b>03 / EXPLIQUER</b><h3>Contexte local</h3><p>Le mécanisme, les usages, le territoire et les limites sont rendus lisibles.</p></div><div class="proof-step"><b>04 / ASSUMER</b><h3>Signature humaine</h3><p>Gabriel vérifie, analyse et approuve chaque publication.</p></div></div></section>`;document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{state.filter=b.dataset.filter;home()})}
function article(slug){const a=publicCorpus().find(x=>x.slug===slug);if(!a)return missing();schema(a);const isDemo=Boolean(a.demo);main.innerHTML=`<article><header class="page-head"><p class="kicker">${esc(a.country)} · ${esc(a.sector)} · ${esc(a.format||'Analyse')}</p><h1>${esc(a.title)}</h1><p class="article-lede">${esc(a.summary)}</p>${isDemo?'<span class="badge">CONTENU PILOTE</span>':''}</header><div class="narrow"><h2>Ce qui s’est passé</h2><p>${esc(a.body)}</p><div class="fact-box"><p class="kicker">CE QUE NOUS POUVONS CONFIRMER</p><p>${esc(a.confirmed)}</p></div><h2>Ce qui reste à vérifier</h2><p class="uncertain">${esc(a.unknown)}</p><h2>L’analyse de Gabriel</h2><p>${esc(a.analysis)}</p><h2>Sources</h2><ul class="sources">${(a.sources||[]).map(s=>`<li><span class="badge">NIVEAU ${esc(s.level)}</span> ${s.url?`<a href="${esc(s.url)}" target="_blank" rel="noopener">${esc(s.name)} ↗</a>`:esc(s.name)}</li>`).join('')||'<li>Source documentée dans le dossier éditorial.</li>'}</ul><aside class="author-box"><div class="author-monogram">GD</div><div><p class="kicker">SYNTHÈSE & ANALYSE</p><h3>Gabriel Emrick DAHISSIHO</h3><p>L’IA assiste la veille. L’auteur contrôle les faits et assume la publication.</p></div></aside></div></article>`}
const trust={methodologie:['La méthode est<br><em>le produit.</em>','Un sujet traverse cinq étapes visibles avant de devenir un article.',[['Collecter sans contourner','RSS, API et pages publiques stables.'],['Qualifier le signal','Pays, secteur, pertinence et niveau de source.'],['Relier les preuves','Une source primaire ou deux confirmations indépendantes.'],['Analyser humainement','Le fait et l’interprétation ne sont jamais confondus.'],['Corriger publiquement','Date, motif et preuve restent visibles.']]],sources:['Nos sources.<br>Nos <em>limites.</em>','Aucune source n’est transformée automatiquement en vérité.',[['Niveau A — primaire','Institution, régulateur, entreprise concernée ou donnée originale.'],['Niveau B — secondaire','Média reconnu ou analyse méthodologiquement transparente.'],['Niveau C — signal','Point de départ uniquement ; insuffisant pour publier.'],['Intelligence artificielle','Elle classe et résume. Elle ne publie, ne signe et n’invente jamais.']]],corrections:['L’erreur se corrige.<br>La trace <em>reste.</em>','Toute correction substantielle affiche sa date, son motif et son origine.',[['Historique public','Aucune correction publiée pour le moment.'],['Droit de réponse','Les organisations citées peuvent apporter de nouveaux éléments, évalués avec les mêmes critères.']]],auteur:['Gabriel Emrick<br><em>DAHISSIHO.</em>','Chef de projet IT, product builder et analyste des usages technologiques.',[['Ce que je construis','Des produits qui relient technologie, gouvernance de l’information et utilité concrète.'],['Ma conviction','L’automatisation augmente notre responsabilité : la méthode doit rester visible.'],['Africa Digital Pulse','Une étude de cas vivante sur la veille, la preuve et la publication responsable.']]]};
function trustPage(id){const p=trust[id];main.innerHTML=`<header class="page-head"><p class="kicker">TRANSPARENCE · CONFIANCE</p><h1>${p[0]}</h1><p>${p[1]}</p></header><div class="narrow">${p[2].map(x=>`<section><h2>${x[0]}</h2><p>${x[1]}</p></section>`).join('')}</div>`}
function listing(title,desc){main.innerHTML=`<header class="page-head"><p class="kicker">EXPLORER LE CORPUS</p><h1>${title}</h1><p>${desc}</p></header><section class="section"><div class="story-grid">${publicCorpus().map(story).join('')}</div></section>`}
async function api(path,opts){const r=await fetch('/api/'+path,opts),data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`Erreur API ${r.status}`);return data}
async function loadStudio(){state.loading=true;try{const [s,src,arts]=await Promise.all([api('signals'),api('sources'),api('articles')]);state.signals=s.items||[];state.sources=src.items||[];state.articles=arts.items||[]}catch(e){console.error(e);toast('Connexion aux données impossible')}finally{state.loading=false;renderStudioTab()}}
function studio(){main.innerHTML=`<div class="studio-shell"><aside class="studio-side"><h2>ADP / STUDIO</h2>${[['overview','Vue générale'],['signals','Signaux collectés'],['analysis','File d’analyse'],['articles','Brouillons & articles'],['sources','Registre sources'],['editor','Nouvel article']].map(([x,l])=>`<button data-tab="${x}" class="${state.tab===x?'active':''}">${l}</button>`).join('')}</aside><section class="studio-main" id="studio-main"></section></div>`;document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{state.tab=b.dataset.tab;if(b.dataset.tab==='editor'){state.editorSignal=null;state.editorArticle=null}studio();if(['overview','signals','analysis','articles','sources'].includes(state.tab))loadStudio()});renderStudioTab();if(!state.signals.length&&!state.loading)loadStudio()}
function renderStudioTab(){const el=document.querySelector('#studio-main');if(!el)return;if(state.tab==='overview'){const fresh=state.signals.filter(x=>x.status==='new').length;el.innerHTML=`<div class="studio-topline"><p class="kicker">CENTRE DE VEILLE</p><button class="button acid" data-refresh ${state.refreshing?'disabled':''}>${state.refreshing?'COLLECTE EN COURS…':'↻ ACTUALISER MAINTENANT'}</button></div><h1>Bonjour,<br>Gabriel.</h1><div class="stats"><div class="stat"><small>SIGNAUX TOTAL</small><b>${state.signals.length}</b></div><div class="stat"><small>À QUALIFIER</small><b>${fresh}</b></div><div class="stat"><small>EN ANALYSE</small><b>${state.signals.filter(x=>x.status==='qualified').length}</b></div><div class="stat"><small>SOURCES ACTIVES</small><b>${state.sources.filter(x=>x.active).length}</b></div></div><div class="pipeline">${[['01','RSS / API'],['02','Déduplication'],['03','Filtre pays'],['04','Qualification'],['05','Approbation']].map(x=>`<div class="pipe"><b>${x[0]}</b>${x[1]}</div>`).join('')}</div>${signalPanel(state.signals.slice(0,6),'Derniers signaux reçus')}`;bindSignalActions()}
 else if(state.tab==='signals'){el.innerHTML=`<div class="studio-topline"><p class="kicker">BOÎTE DE RÉCEPTION</p><button class="button acid" data-refresh ${state.refreshing?'disabled':''}>${state.refreshing?'COLLECTE EN COURS…':'↻ ACTUALISER MAINTENANT'}</button></div><h1>Signaux<br>collectés.</h1>${filtersPanel()}${signalPanel(filteredSignals(),'File de qualification')}`;bindFilters();bindSignalActions()}
 else if(state.tab==='analysis'){const queued=state.signals.filter(x=>x.status==='qualified');el.innerHTML=`<div class="studio-topline"><p class="kicker">WORKFLOW ÉDITORIAL</p><span class="sync-state">${queued.length} EN ATTENTE</span></div><h1>File<br>d’analyse.</h1><p>Ouvrez un signal pour transformer ses informations sourcées en article éditable.</p>${signalPanel(queued,'Sujets qualifiés pour analyse')}`;bindSignalActions()}
 else if(state.tab==='articles'){el.innerHTML=`<div class="studio-topline"><p class="kicker">PRODUCTION ÉDITORIALE</p><span class="sync-state">${state.articles.length} ARTICLE(S)</span></div><h1>Brouillons<br>& articles.</h1>${articlesPanel()}`;bindArticleActions()}
 else if(state.tab==='sources')el.innerHTML=`<p class="kicker">PROVENANCE</p><h1>Registre<br>des sources.</h1><div class="panel"><div class="table-wrap"><table><thead><tr><th>Source</th><th>Niveau</th><th>Type</th><th>Dernière collecte</th><th>État</th></tr></thead><tbody>${state.sources.map(s=>`<tr><td class="signal-title"><span class="source-dot ${s.level==='A'?'':'secondary'}"></span>${esc(s.name)}</td><td><span class="badge">${esc(s.level)}</span></td><td>${esc(s.kind)}</td><td>${fmtDate(s.last_fetched_at)}</td><td><span class="state">${s.active?'ACTIVE':'PAUSE'}</span></td></tr>`).join('')||'<tr><td colspan="5" class="empty">Le registre sera rempli après la première collecte.</td></tr>'}</tbody></table></div></div>`;
 else editor(el)}
function filtersPanel(){const opt=(items,value)=>`<option value="">Tous</option>${items.filter(Boolean).filter((x,i,a)=>a.indexOf(x)===i).sort().map(x=>`<option ${x===value?'selected':''}>${esc(x)}</option>`).join('')}`;return `<div class="filters-panel"><label>Pays<select data-filter-key="country">${opt(state.signals.map(x=>x.country),state.filters.country)}</select></label><label>Source<select data-filter-key="source">${opt(state.sources.map(x=>x.name),state.filters.source)}</select></label><label>Secteur<select data-filter-key="sector">${opt(state.signals.map(x=>x.sector),state.filters.sector)}</select></label><label>Statut<select data-filter-key="status"><option value="">Tous</option><option value="new" ${state.filters.status==='new'?'selected':''}>À qualifier</option><option value="qualified" ${state.filters.status==='qualified'?'selected':''}>En analyse</option></select></label><label>Période<select data-filter-key="period"><option value="7" ${state.filters.period==='7'?'selected':''}>7 derniers jours</option><option value="30" ${state.filters.period==='30'?'selected':''}>30 derniers jours</option><option value="90" ${state.filters.period==='90'?'selected':''}>90 derniers jours</option><option value="all" ${state.filters.period==='all'?'selected':''}>Toutes les dates</option></select></label><label>Tri<select data-filter-key="sort"><option value="newest" ${state.filters.sort==='newest'?'selected':''}>Plus récent d’abord</option><option value="oldest" ${state.filters.sort==='oldest'?'selected':''}>Plus ancien d’abord</option></select></label></div>`}
function filteredSignals(){const f=state.filters,cut=f.period==='all'?0:Date.now()-Number(f.period)*86400000;return state.signals.filter(x=>(!f.country||x.country===f.country)&&(!f.source||x.source_name===f.source)&&(!f.sector||x.sector===f.sector)&&(!f.status||x.status===f.status)&&(!cut||new Date(x.published_at||x.collected_at).getTime()>=cut)).sort((a,b)=>(f.sort==='oldest'?1:-1)*(new Date(a.published_at||a.collected_at)-new Date(b.published_at||b.collected_at)))}
function signalPanel(items,title){const lastFetch=state.sources.map(s=>s.last_fetched_at).filter(Boolean).sort().at(-1);return `<div class="panel"><div class="panel-head"><h2>${title}</h2><span class="kicker">${items.length} ÉLÉMENTS · SOURCES VÉRIFIÉES ${fmtDate(lastFetch)}</span></div><div class="table-wrap"><table><thead><tr><th>Signal</th><th>Source</th><th>Pays détecté</th><th>Publié</th><th>Première collecte</th><th>Dernière vérification</th><th>État / Action</th></tr></thead><tbody>${items.map(s=>`<tr><td class="signal-title"><a href="${esc(s.url)}" target="_blank" rel="noopener">${esc(s.title)} ↗</a><small>${esc(s.sector||'Secteur à qualifier')}</small></td><td>${esc(s.source_name)} <span class="badge">${esc(s.source_level)}</span></td><td>${esc(s.country||'À qualifier')}</td><td>${fmtDate(s.published_at)}</td><td>${fmtDate(s.collected_at)}</td><td>${fmtDate(s.last_seen_at||s.collected_at)}</td><td>${s.status==='new'?`<button class="analyse-btn" data-analyse="${s.id}">Envoyer en analyse →</button>`:`<button class="analyse-btn" data-edit-signal="${s.id}">Ouvrir dans l’éditeur →</button>`}</td></tr>`).join('')||`<tr><td colspan="7" class="empty">Aucun signal ne correspond à ces critères.</td></tr>`}</tbody></table></div></div>`}
function bindFilters(){document.querySelectorAll('[data-filter-key]').forEach(x=>x.onchange=()=>{state.filters[x.dataset.filterKey]=x.value;renderStudioTab()})}
function bindSignalActions(){document.querySelectorAll('[data-refresh]').forEach(b=>b.onclick=refreshNow);document.querySelectorAll('[data-analyse]').forEach(b=>b.onclick=()=>sendToAnalysis(Number(b.dataset.analyse),b));document.querySelectorAll('[data-edit-signal]').forEach(b=>b.onclick=()=>openSignalEditor(Number(b.dataset.editSignal)))}
async function refreshNow(){if(state.refreshing)return;state.refreshing=true;renderStudioTab();try{const r=await api('refresh',{method:'POST'});toast(`Veille Afrique : ${r.inserted||0} nouveau(x), ${r.relevant||0} pertinent(s), ${r.fetched||0} lus`);await loadStudio()}catch(e){console.error(e);toast(e.message||'La collecte n’a pas pu être relancée')}finally{state.refreshing=false;renderStudioTab()}}
async function sendToAnalysis(id,button){button.disabled=true;button.textContent='ENVOI…';try{await api('analyse',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({signalId:id})});const s=state.signals.find(x=>x.id===id);if(s)s.status='qualified';toast('Signal ajouté à la file d’analyse');renderStudioTab()}catch(e){button.disabled=false;button.textContent='Réessayer';toast('Envoi impossible')}}
function openSignalEditor(id){state.editorSignal=state.signals.find(x=>x.id===id)||null;state.editorArticle=state.articles.find(x=>x.signal_id===id)||null;state.tab='editor';studio()}
function articlesPanel(){return `<div class="panel"><div class="table-wrap"><table><thead><tr><th>Titre</th><th>Pays / secteur</th><th>Statut</th><th>Dernière modification</th><th>Action</th></tr></thead><tbody>${state.articles.map(a=>`<tr><td class="signal-title">${esc(a.title)}</td><td>${esc(a.country||'—')} · ${esc(a.sector||'—')}</td><td><span class="state ${a.status==='published'?'qualified':''}">${a.status==='published'?'PUBLIÉ':'BROUILLON'}</span></td><td>${fmtDate(a.updated_at)}</td><td><button class="analyse-btn" data-edit-article="${a.id}">Modifier →</button></td></tr>`).join('')||'<tr><td colspan="5" class="empty">Aucun brouillon. Ouvrez un signal depuis la file d’analyse.</td></tr>'}</tbody></table></div></div>`}
function bindArticleActions(){document.querySelectorAll('[data-edit-article]').forEach(b=>b.onclick=()=>{state.editorArticle=state.articles.find(x=>x.id===Number(b.dataset.editArticle))||null;state.editorSignal=state.signals.find(x=>x.id===state.editorArticle?.signal_id)||null;state.tab='editor';studio()})}
function editor(el){const s=state.editorSignal||{},a=state.editorArticle||{},pick=(value,items)=>items.map(x=>`<option ${x===value?'selected':''}>${x}</option>`).join('');el.innerHTML=`<div class="studio-topline"><p class="kicker">APPROBATION HUMAINE</p><span class="sync-state">${a.status==='published'?'ARTICLE PUBLIÉ':a.id?'BROUILLON D1':'NOUVEAU BROUILLON'}</span></div><h1>${a.id?'Modifier':'Construire'}<br>l’article.</h1>${s.id?`<aside class="source-context"><b>SIGNAL SOURCE · ${esc(s.source_name)}</b><a href="${esc(s.url)}" target="_blank" rel="noopener">${esc(s.title)} ↗</a><p>Publié ${fmtDate(s.published_at)} · collecté ${fmtDate(s.collected_at)}</p></aside>`:''}<form id="editor"><div class="form-grid"><div class="field full"><label>TITRE FACTUEL</label><input required name="title" value="${esc(a.title||s.title||'')}"></div><div class="field"><label>PAYS</label><select name="country">${pick(a.country||s.country||'Côte d’Ivoire',['Côte d’Ivoire','Bénin','Sénégal'])}</select></div><div class="field"><label>SECTEUR</label><select name="sector">${pick(a.sector||s.sector||'Fintech',['Fintech','Santé','Agriculture','Éducation','Services publics','Télécommunications','Cybersécurité','Intelligence artificielle'])}</select></div><div class="field full"><label>RÉSUMÉ — 3 À 5 LIGNES</label><textarea required name="summary">${esc(a.summary||s.summary||'')}</textarea></div><div class="field full"><label>CE QUI S’EST PASSÉ</label><textarea required name="body">${esc(a.body||'')}</textarea></div><div class="field"><label>CE QUI EST CONFIRMÉ</label><textarea required name="confirmed">${esc(a.confirmed||'')}</textarea></div><div class="field"><label>CE QUI RESTE À VÉRIFIER</label><textarea required name="unknown">${esc(a.unknown_facts||'')}</textarea></div><div class="field full"><label>ANALYSE ORIGINALE DE GABRIEL</label><textarea required name="analysis">${esc(a.analysis||'')}</textarea></div></div><fieldset class="checklist"><legend>Contrôles obligatoires avant publication</legend>${['Source primaire présente ou absence justifiée','Noms, dates et chiffres contrôlés','Faits et analyse clairement séparés','Limites et incertitudes visibles'].map(x=>`<label><input type="checkbox" name="check"> ${x}</label>`).join('')}<label><input type="checkbox" name="approval"> J’approuve personnellement cette publication</label></fieldset><div class="actions"><button class="button" type="button" id="draft">Enregistrer le brouillon</button><button class="button acid" type="submit" disabled>Publier l’article</button></div></form>`;const f=el.querySelector('form'),checks=[...f.querySelectorAll('input[type=checkbox]')],pub=f.querySelector('[type=submit]');f.oninput=()=>pub.disabled=!(checks.every(x=>x.checked)&&f.checkValidity());const save=async status=>{const fd=new FormData(f),payload={id:a.id||null,signalId:s.id||a.signal_id||null,status,approved:status==='published',title:fd.get('title'),country:fd.get('country'),sector:fd.get('sector'),summary:fd.get('summary'),body:fd.get('body'),confirmed:fd.get('confirmed'),unknown:fd.get('unknown'),analysis:fd.get('analysis'),sourceUrl:s.url||a.source_url||'',sourceName:s.source_name||a.source_name||''};try{const out=await api('articles',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});state.editorArticle=out.item;toast(status==='published'?'Article publié après validation':'Brouillon enregistré dans D1');await loadStudio()}catch(e){toast('Enregistrement impossible')}};f.onsubmit=e=>{e.preventDefault();save('published')};f.querySelector('#draft').onclick=()=>save('draft')}
function fmtDate(s){if(!s)return'Jamais';try{return new Intl.DateTimeFormat('fr-FR',{dateStyle:'short',timeStyle:'short'}).format(new Date(s))}catch{return s}}
function missing(){main.innerHTML='<header class="page-head"><p class="kicker">404</p><h1>Signal<br>introuvable.</h1></header>'}
async function route(){scrollTo(0,0);if(location.pathname.replace(/\/$/,'')==='/studio')return studio();await loadPublicArticles();const [_,p,arg]=(location.hash||'#/').split('/');if(!p)home();else if(p==='article')article(arg);else if(p==='analyses')listing('Toutes les<br><em>analyses.</em>','Des faits mis en perspective avec leurs implications et leurs limites.');else if(p==='pays')listing('Trois pays.<br>Un périmètre <em>assumé.</em>','Côte d’Ivoire, Bénin et Sénégal pour le pilote initial.');else if(trust[p])trustPage(p);else missing()}
document.querySelector('.menu').onclick=e=>{const n=document.querySelector('nav'),o=n.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',o)};addEventListener('hashchange',route);route();
